# a function that takes a list of strings, and returns a string that is all those strings concatenated together
# input: list (of strings)
# output: a single string with all the string concatenated
# use .join method 
# use ", " as a seperator
# RETURN the long string

def list_to_string(list_of_strings):
    return " ".join(list_of_strings)